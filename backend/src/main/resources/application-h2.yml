spring:
  datasource:
    url: jdbc:h2:file:${user.home}/.timetable-scheduler/data/timetabledb;DB_CLOSE_DELAY=-1;MODE=PostgreSQL;NON_KEYWORDS=VALUE
    username: sa
    password:
    driver-class-name: org.h2.Driver

  h2:
    console:
      enabled: true
      path: /h2-console
      settings:
        web-allow-others: true

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    open-in-view: false
    properties:
      hibernate:
        dialect: org.hibernate.dialect.H2Dialect
        format_sql: true
        highlight_sql: true

  flyway:
    enabled: false

app:
  upload:
    dir: ${UPLOAD_DIR:./uploads}
  jwt:
    secret: TimetableSchedulerSuperSecretKey2025!!PleaseChangeInProduction64ByteMinimum
    expiration-ms: 86400000          # 24 hours
    refresh-expiration-ms: 604800000  # 7 days
  cors:
    allowed-origins: http://localhost:3000,http://localhost:5173,http://localhost:8080

logging:
  level:
    com.erp.timetable: DEBUG
    org.springframework.web: INFO
    org.springframework.security: INFO
    org.hibernate.SQL: DEBUG
    org.hibernate.orm.jdbc.bind: TRACE

management:
  endpoints:
    web:
      exposure:
        include: health,info,shutdown
  endpoint:
    shutdown:
      enabled: true
    health:
      show-details: always
